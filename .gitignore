# Reference node build artifact (platform-specific binary).
# Build it with examples/build-pollen-node.sh.
/examples/pollen-node

# amc local package cache
/.amalgame-cache/

# v3 validator smoke test binary (built by tests/build-v3-validator-smoke.sh)
/tests/v3_validator_smoke

# CEL-lite smoke test binary (built by tests/build-cel-lite-smoke.sh)
/tests/cel_lite_smoke
